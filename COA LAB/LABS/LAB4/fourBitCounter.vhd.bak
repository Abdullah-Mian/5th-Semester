library ieee;
use ieee.std_logic_1164.all;
use work.MyPackage.all;

entity fourBitCounter is
    port (
        MAX10_CLK1_50 : in std_logic;  -- 50MHz clock input
        KEY : in std_logic_vector(1 downto 0);  -- KEY[0] will be used for reset
        LEDR : out std_logic_vector(3 downto 0)  -- 4 LEDs for output display
    );
end fourBitCounter;

architecture Behavioral of fourBitCounter is
    -- You can add any signals you need here
begin
    -- Your code will go here
end Behavioral;